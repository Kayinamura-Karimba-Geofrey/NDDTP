services:
  nddp-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '5173:80'
    environment:
      - VITE_API_GATEWAY_URL=http://host.docker.internal:3000/api/svc
    restart: unless-stopped
